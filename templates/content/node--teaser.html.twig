
{#
  2023-12-06 :: Teaser is not a card. It is a col-md-12 sort of a display.
#}

{%
  set classes = [
    'Teaser',
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
  ]
%}
{{ attach_library('ish_drupal_theme/node') }}
<article{{ attributes.addClass(classes) }}>

  <div class='d-flex'>
    <div class='mr-2' style="" >
      {{ content.field_vote_up_down }}
    </div>
    <div class='d-flex lvl-1 flex-column align-items-start'>

        {{ title_prefix }}
        {% if label and not page %}
          <h2{{ title_attributes }}>
            <a href="{{ url }}" rel="bookmark">{{ label }}</a>
          </h2>
        {% endif %}
        {{ title_suffix }}

        <div class='thumb' >
          {% if content.field_image_thumb_url | length > 3 %}
            <a href="{{ url }}" >
              <img class='image-thumb' src="{{- content.field_image_thumb_url|escape|striptags|replace({"\n": "", "\r": "", "\t": ""}) -}}" alt='' />
            </a>
          {% elseif content.field_image_thumb %}
            <a href="{{ url }}" >
              {{ content.field_image_thumb }}
            </a>
          {% endif %}
        </div>

        <div{{ content_attributes.addClass('node__content') }}>
          {{ content | without('field_vote_up_down') | without('field_image_thumb') | without('field_image_thumb_url') }}
          {% if content.field_example is defined %}
            ({{ node.field_comments.comment_count |default(0) }} {{ 'Comments'|t }})
          {% endif %}
        </div>

        {% if display_submitted %}
          <footer class="node__meta">
            <div{{ author_attributes.addClass('node__submitted') }}>
              {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}
              {{ metadata }}
            </div>
          </footer>
        {% endif %}

    </div>
  </div>



  <div class="clearfix"></div>

</article>

