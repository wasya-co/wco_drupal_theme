
{% if content %}
    {% set classes = [
      'region',
      'region-' ~ region|clean_class,
    ] %}
    <div{{ attributes.addClass(classes) }}>

      <div class='maxwidth'>
        {{ content }}
      </div>

    </div>
{% endif %}
