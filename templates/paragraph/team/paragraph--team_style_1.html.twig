<section class="home1 home1-team">
    <div class="container">
        <div class="home1-team-block">
            <div class="text-center aos-init aos-animate" data-aos="zoom-in">
                <span class="mini-title">{{ content.field_tagline.0 }}</span>
                <h2 class="center-title">{{ content.field_title.0 }}</h2>
            </div>
            {{ content.field_team_members }}          
            <a href="{{ content.field_link.0['#url'] }}" title="{{ content.field_link.0['#title'] }}" class="btn btn-primary center-btn">{{ content.field_link.0['#title'] }}</a> 
        </div>
    </div>
</section>