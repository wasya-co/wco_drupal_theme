<?php

/* function wco_drupal_theme_preprocess_block(&$variables) {
  // var_dump( $variables );

  if (isset($variables['block']->subject) && !empty($variables['block']->subject)) {
    array_unshift($variables['theme_hook_suggestions'], 'block__' . str_replace(' ', '_', drupal_strtolower($variables['block']->subject)));
  }
} */



